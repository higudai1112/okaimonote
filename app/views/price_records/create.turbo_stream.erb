<% if @price_record.errors.any? %>
  <%= turbo_stream.replace "price_record_form" do %>
    <%= turbo_frame_tag "price_record_form" do %>
      <%= render "form_content", price_record: @price_record, mode: @mode %>
    <% end %>
  <% end %>
<% end %>