<div class="min-h-screen bg-base flex items-center justify-center px-4 sm:px-6 md:px-10 py-10">
  <div class="bg-white rounded-2xl shadow-lg border border-orange-100 p-6 sm:px-8 w-full max-w-md sm:max-w-lg">

    <!-- 見出し -->
    <h2 class="text-xl sm:text-2xl font-bold text-center text-orange-500 mb-6">
      ファミリーを作成
    </h2>

    <!-- フォーム -->
    <%= form_with model: @family, url: family_path, local: true do |f| %>

      <!-- 家族名 -->
      <div class="mb-5">
        <%= f.label :name, "グループ名", class: "block text-gray-700 font-semibold mb-2" %>
        <%= f.text_field :name,
              placeholder: "田中ファミリー",
              class: "w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-orange-300 focus:border-orange-400 outline-none transition placeholder-gray-400" %>
      </div>

      <!-- 作成ボタン -->
      <%= f.submit "作成する",
           class: "w-full bg-orange-500 text-white font-semibold py-3 rounded-lg shadow hover:bg-orange-600 transition-transform active:scale-[0.98]" %>
    <% end %>

    <div class="text-center mt-4">
      <%= link_to "戻る", settings_path, class: "text-sm text-gray-500 hover:text-gray-700 underline" %>
    </div>
  </div>
</div>