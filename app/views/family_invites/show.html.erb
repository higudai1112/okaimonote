<div class="min-h-screen bg-base flex items-center justify-center px-4">
  <div class="bg-white shadow-xl border border-orange-100 rounded-2xl p-8 max-w-md w-full">

    <h1 class="twxt-2xl font-bold text-orange-500 mb-6 text-center">
      ファミリーに加わりますか？
    </h1>

    <p class="text-gray-700 text-center mb-6">
     「<%= @family.name %>」に参加しようとしています。
    </p>

    <%= form_with url: family_invite_path(@family.invite_token), method: :post, local: true do %>
      <button class="w-full bg-orange-500 text-white font-semibold py-3 rounded-full shadow hover:bg-orange-600 transition">
        メンバーになる
      </button>
    <% end %>

    <div class="text-center mt-4">
      <%= link_to "キャンセル", settings_path, class:"text-gray-500 underline" %>
    </div>

  </div>
</div>
