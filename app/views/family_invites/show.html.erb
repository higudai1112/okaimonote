<div class="min-h-screen bg-base flex items-center justify-center px-4 sm:px-6 md:px-10 py-10">
  <div class="bg-white shadow-xl border border-orange-100 rounded-2xl p-6 sm:p-8 max-w-md sm:max-w-md w-full">

    <h1 class="text-xl sm:text-2xl font-bold text-orange-500 mb-6 text-center">
      ファミリーに加わりますか？
    </h1>

    <p class="text-gray-700 text-center mb-6 leading-relaxed">
     「<%= @family.name %>」に参加しようとしています。
    </p>

    <%= form_with url: family_invite_path(@family.invite_token), method: :post, local: true do %>
      <button class="w-full bg-orange-500 text-white font-semibold py-2.5 rounded-full shadow hover:bg-orange-600 transition-transform active:scale[0.98]">
        メンバーになる
      </button>
    <% end %>

    <div class="text-center mt-6">
      <%= link_to "キャンセル", settings_path, class:"text-sm text-gray-500 hover:text-gray-700 underline transition" %>
    </div>

  </div>
</div>
